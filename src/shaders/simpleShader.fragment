//[FRAGMENT SHADER]
#version 330 core

in VERTEXOUT{
    vec4 Color;
    vec3 FragPos;
    vec3 Normal;
    vec2 TexCoord;
    float Blend;
    vec4 LightFragPos;
}fragmentin;

void main(){
    gl_FragColor=vec4(0.0);
}
